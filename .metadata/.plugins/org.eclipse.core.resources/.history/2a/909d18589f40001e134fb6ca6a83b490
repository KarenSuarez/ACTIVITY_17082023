package co.edu.uptc;

import co.edu.uptc.model.TaskManager;
import co.edu.uptc.presenter.TaskManagerPresenter;
import co.edu.uptc.view.TaskManagerApp;

public class Main {
    public static void main(String[] args) {
        TaskManager model = new TaskManager();
        TaskManagerApp view = new TaskManagerApp();
        TaskManagerPresenter presenter = new TaskManagerPresenter(model, view);
    }
}